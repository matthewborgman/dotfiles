#!/usr/bin/env bash

# Define custom aliases for use with personal projects

if [ -d "$WEBSITES_PATH" ]; then

	alias puma="cd $WEBSITES_PATH/go.puma.com"
	alias sungem="cd $WEBSITES_PATH/sungemcms.com"
	alias websites="cd $WEBSITES_PATH"
fi